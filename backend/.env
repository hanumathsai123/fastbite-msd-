# Example env for fastbite backend
# Copy this to .env and fill values for your environment

# Server
PORT=4000

# MySQL / MariaDB
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=fastbite

# JSON Web Token secret (change in production)
JWT_SECRET=replace_with_a_strong_secret
