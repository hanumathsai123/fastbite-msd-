FastBite — Food Delivery Project (Submission Zip)
================================================
What's included:
- backend/: Minimal Express + MySQL-ready scaffold (auth, restaurants, cart, orders)
- frontend/: Minimal React (Vite) scaffold with Tailwind + Framer Motion placeholders
- sql/seed.sql: Create tables + sample seed data
- .env.example: environment variables example

How to run (backend):
1. cd backend
2. npm install
3. create .env with DB and JWT keys (see .env.example)
4. node src/index.js

How to run (frontend):
1. cd frontend
2. npm install
3. npm run dev

Notes:
- This is a submission scaffold. Replace API keys (Stripe etc.) and complete UI as needed.
